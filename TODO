# smoother todo (ranked)

- ploidy: 
    - import new ploidy file in interface
    - GUI for ploidy?
- how about a RPKB (reads per kilobase) normalization?
- remove default.json from smoother_hg
- slices normalisation not working with mouse radicl-seq dataset